from datetime import datetime

import bot
import database
from schpincer import Opening
from util import format_date

# Main entry point for Kamilla
if __name__ == '__main__':
    # database.init_table()
    # o = Opening
    # o = datetime(2020, 1, 1, 12, 0, 0)
    # o.orderable = True
    # o.circleName = "Pizzásch"
    # database.insert_opening(o)
    #bot.start()
    print(format_date(o))
